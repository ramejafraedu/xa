# Video Factory V16 — Dependencies
# Install: pip install -r requirements.txt

# Core
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
python-dotenv>=1.0,<2
loguru>=0.7,<1
typer>=0.15,<1
rich>=13.9,<14
pyyaml>=6.0,<7          # V16: YAML niche manifests (nichos/*.yaml)
jsonschema>=4.20,<5     # OpenMontage styles/playbook validation

# HTTP (single library, no mixing)
httpx>=0.28,<1

# LangChain stack (used by multiple agents)
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4

# TTS
edge-tts>=6.1,<7

# Scheduler
apscheduler>=3.10,<4

# CLI/UI
rich>=13.0.0
typer>=0.9.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Memory Management
psutil>=6.1,<7
networkx>=3.2,<4          # Semantic memory graph export (agents.semantic_memory)

# Dashboard
fastapi>=0.115,<1
uvicorn>=0.34,<1
sse-starlette>=2.2,<3
psutil>=6.1,<7

# Video/Audio (optional, for async file ops)
aiofiles>=23.2,<24
yt-dlp>=2025.1
opencv-python-headless>=4.10,<5
scenedetect>=0.6,<0.7
moviepy>=2,<3

# Gemini API (Veo 3.1, Lyria 3, Imagen, TTS)
google-genai>=1.14
google-cloud-aiplatform>=1.60.0
google-cloud-texttospeech>=2.17.2
google-auth>=2.36.0

# WhisperX — word-level subtitles (optional, heavy install)
# pip install whisperx  (requires torch, ~2GB)
# Uncomment only if you want precise subtitle timing:
# whisperx>=3.1

# Trends (optional, may fail due to Google blocks)
# pytrends>=4.9,<5

# Google Drive/Sheets (optional — enable when ready)
# google-api-python-client>=2.160
# google-auth>=2.37
# google-auth-oauthlib>=1.2

# Supabase (optional — enable when ready)
# supabase>=2.8

# Crew AI (V16 PRO Enterprise Quality Gate)
crewai>=0.80,<1

# Manim (Optional — V16 PRO Financial Data Viz)
# Requires apt-get install ffmpeg libcairo2-dev libpango1.0-dev texlive
# manim>=0.19
